version: '3.8'

services:
  api:
    container_name: "kotts_backend"
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - api-data:/app/
    ports:
      - "8080:8080"
networks:
  default:

volumes:
  api-data: